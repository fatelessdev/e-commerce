## 2024-06-01 - Missing ARIA Labels on Icon-only Buttons\n**Learning:** Interactive components like navbar controls (search, menu) and custom swatches (color selectors) often lack accessible names if they solely rely on visual SVG icons or CSS background colors.\n**Action:** Always verify that `<button>` tags relying on visual indicators have an explicit `aria-label` or `.sr-only` text.
